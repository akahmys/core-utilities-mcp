# Project Roadmap & Execution Log

## 🗺️ Roadmap (Completed Milestones)

### Phase 1: Workspace & Guardrails Implementation
*   [x] **AWU 1.1: Workspace Cargo setup**
*   [x] **AWU 1.2: Path safety & Smart Output Truncation Guardrails**
*   [x] **AWU 1.3: Safe Delete and Read File with Limit Prototype**

### Phase 2: Full Tool Suite & MCP Server
*   [x] **AWU 2.1: Implement MCP wrapper and tool definitions**
*   [x] **AWU 2.2: Implement remaining core uutils/coreutils tools**
*   [x] **AWU 2.3: Execution Sandbox** (~~Rollback Manager~~ — see correction below)
*   [x] **AWU 2.4: Reinstall renamed mcp-uutils binary**
*   [x] **AWU 2.5: Rename project to core-utilities-mcp**

---

## 🎯 Short-Term Plan

### Phase 8: Deep Refactoring & Stream Optimization
*   [x] **AWU 8.1: Fix Potential Stream Truncation Bug in `sys_ops::read_output_streams`**
*   [x] **AWU 8.2: Unique Temporary File Names in `file_ops::mutate::atomic_write`**
*   [x] **AWU 8.3: Memory & Stream Optimization for `search_ops` & `text_ops::read_file_with_limit`**
*   [x] **AWU 8.4: Path Verification & Existence Helper (Boilerplate Reduction)**
*   [x] **AWU 8.5: Streamlined Deserialization in `dispatch.rs` (Default Derive)**
*   [x] **AWU 8.6: Apply `ENV_MUTEX` Lock in `property_tests.rs` & Add `write_file` Integration Test**
*   [x] **AWU 8.7: Reorder `README.md` sections to place Core Command Specification after Overview**
*   [x] **AWU 8.8: Reorder `README.md` to user-centric flow (Command Spec -> Guardrails -> Repo Structure -> Setup & Test)**
*   [x] **AWU 8.9: Fix CI Clippy Failure from Upstream Stable-Toolchain Drift**
*   [x] **AWU 8.10: Absorb `file-edit-mcp`'s Edit/Read Improvements — `edit_file`/`read_file`**
*   [x] **AWU 8.11: Command Composition Review — Fix `search_text` Trim Bug, Rename 5 Tools, Add TOML/YAML Support**
*   [x] **AWU 8.12: Replace `serde_yaml` with `yaml-rust2` (Pure-Rust, No Unsound-Fork Risk)**
*   [x] **AWU 8.13: Full Codebase Audit (Incl. Tests) — File/Function-Length Splits, Dead `CoreError` Variants, Stale Docs**
*   [x] **AWU 8.14: Documentation & Policy-File Review — Fix `uutils/coreutils` Claim, Sync `PLANS.md` Status**

*   [x] **AWU 4.4: Refactor `tools/call` Error Handling**
*   [x] **AWU 3.1: Fix Clippy Warnings** (needless_borrows_for_generic_args)
*   [x] **AWU 3.2: Apply Standard Formatting** (cargo fmt)
*   [x] **AWU 3.3: Dependency Audit** (cargo audit - *Skipped: tool not in environment*)

### Phase 2: Library Robustness (Completed)
*   [x] **AWU 4.1: Implement Structured Error Handling** (thiserror)
*   [x] **AWU 4.2: Enhance Guardrails & Safety**
*   [x] **AWU 4.3: Documentation & API Design** (doc comments & examples)

### Phase 3: Server Maturity (Completed)
*   [x] **AWU 5.1: Integrate Structured Logging** (tracing)
*   [x] **AWU 5.2: Refine JSON-RPC Loop** (graceful shutdown)
*   [x] **AWU 5.3: Add Integration Testing** (end-to-end JSON-RPC)

### Phase 4: Quality Assurance & Automation (Completed)
*   [x] **AWU 6.1: Expand Test Coverage** (unit, integration, property)
*   [x] **AWU 6.2: CI/CD Pipeline Setup** (GitHub Actions)

### Phase 5: Design Review Follow-ups (Completed)
*   [x] **AWU 7.1: Optional Workspace-Root Confinement** (`AI_WORKSPACE_ROOT`)
*   [x] **AWU 7.2: Correct "Sandbox" Terminology to Match Reality**
*   [x] **AWU 7.3: Remove `extract_code_skeleton`** (scope: out of "core utilities")
*   [x] **AWU 7.4: `execute_command_in_sandbox` Timeout & Working-Directory Support**
*   [x] **AWU 7.5: Rename `execute_command_in_sandbox` → `execute_command`**

---

## 📝 Execution Log

| AWU ID | Task Name | Status | Result / Notes |
| :--- | :--- | :--- | :--- |
| **1.1** | **Workspace Cargo setup** | `[✅] Completed` | Converted crate to a Cargo Workspace layout. |
| 1.2 | Path safety & Smart Output Truncation | `[✅] Completed` | Implemented validate_path_safety and truncate_output. |
| 1.3 | Safe Delete and Read File with Limit | `[✅] Completed` | Implemented delete_file_or_directory and read_file_with_limit. |
| 2.1 | MCP Server integration | `[✅] Completed` | Integrated all tools into JSON-RPC schema. |
| 2.2 | Full Tool Suite | `[✅] Completed` | Implemented all 14 core tools. |
| 2.3 | Rollback / Sandbox Integration | `[⚠️] Corrected` | Original entry claimed a "Rollback Manager" was completed; no such code (backup-before-delete, undo log) has ever existed in the repo — only the sandbox runner with stdout guard (`execute_command_in_sandbox`) was actually built. Surfaced during the 2026-07-27 design review; the user decided a rollback manager is not needed, so this will not be implemented. Entry corrected rather than deleted, per this project's own history-integrity rule. |
| 2.4 | Reinstall renamed mcp-uutils binary | `[✅] Completed` | Successfully reinstalled mcp-uutils to cargo bin. |
| 2.5 | Rename project to core-utilities-mcp | `[✅] Completed` | Successfully renamed directories, workspace crates (`core-utilities-mcp` & `core-utilities-mcp-lib`), Rust imports, binary output, license script, and all documentation. Installed binary to `~/.cargo/bin/core-utilities-mcp`. |
| 3.0 | Synthesize Technical Audit & Refactoring Plan | `[✅] Completed` | Plan generated and added to roadmap. |
| **3.1** | **Fix Clippy Warnings** | `[✅] Completed` | Fixed `needless_borrows_for_generic_args` in `sys_ops/mod.rs`. |
| **3.2** | **Apply Standard Formatting** | `[✅] Completed` | Ran `cargo fmt`. |
| **3.3** | **Dependency Audit** | `[✅] Completed` | *Skipped: tool not in environment.* |
| **4.1** | **Implement Structured Error Handling** | `[✅] Completed` | Added `errors.rs` with `thiserror`-based `CoreError`/`CoreResult`. Migrated `file_ops`, `guardrails`, `search_ops`, `text_ops`, and `sys_ops` off `Result<_, String>`. |
| **4.4** | **Refactor `tools/call` Error Handling** | `[✅] Completed` | All `tools/call` arms now return `Result<String, CoreError>`; error responses include `error_type` from `CoreError::category()`. Verified via manual JSON-RPC smoke test and `cargo test --workspace` (19 passed). |
| **4.2** | **Enhance Guardrails & Safety** | `[✅] Completed` | `validate_path_safety` now also rejects NUL-byte paths and exact (case-insensitive) matches against a deny-list of critical system directories (`/etc`, `/bin`, `/sbin`, `/usr`, `/boot`, `/dev`, `/proc`, `/sys`, `/root`, `/System`, `/Library`, `C:\Windows`, `C:\Program Files`); subpaths remain permitted. Corrected `execute_command_in_sandbox` docs to no longer claim an enforced memory limit (only timeout + output-size kill switch exist). README updated. |
| **4.3** | **Documentation & API Design** | `[✅] Completed` | Added crate-level (`//!`) and per-module doc comments across `guardrails`, `file_ops`, `search_ops`, `text_ops`, `sys_ops`, each with `# Errors` and `# Examples` sections. `cargo test --doc` grew from 0 to 17 passing doctests. |
| **5.1** | **Integrate Structured Logging** | `[✅] Completed` | Added `tracing`/`tracing-subscriber` to `core-utilities-mcp`. Logs (request method, tool name, `error_type` on failure, startup/shutdown) go to stderr only, gated by `RUST_LOG` (default `info`); stdout remains pure JSON-RPC. Verified via manual stdout/stderr separation test. README documents `RUST_LOG`. |
| **5.2** | **Refine JSON-RPC Loop** | `[✅] Completed` | Switched stdin reading to async (`tokio::io::stdin` + `AsyncBufReadExt`) so a `Ctrl+C`/`SIGTERM` (`shutdown_signal()`) can interrupt a blocked read via `tokio::select!`. Discovered and fixed a real hang: since `tokio::io::stdin()` reads via a blocking OS thread, returning normally from `main` after a signal made the implicit `Runtime` Drop wait forever for that thread (reproduced with a FIFO held open, simulating a live MCP client connection) — fixed via `std::process::exit(0)` on the signal path. Verified: SIGTERM now exits in ~100ms even mid-read; stdin-EOF and normal request/response flows unaffected (21 unit + 17 doctests still pass). |
| **5.3** | **Add Integration Testing** | `[✅] Completed` | Added `core-utilities-mcp/tests/integration_test.rs` (157 lines): spawns the real compiled binary and drives it over actual stdin/stdout pipes. 7 tests cover `initialize`/`tools/list` (asserts all 15 tools), a successful `tools/call`, a failing `tools/call` asserting `error_type`, malformed-JSON parse errors, unknown-method errors, notification-produces-no-output-line, and graceful exit on stdin close. All pass; `cargo fmt`/`clippy` clean. |
| **6.1** | **Expand Test Coverage** | `[✅] Completed` | Added `proptest` dev-dependency and `core-utilities-mcp-lib/tests/property_tests.rs` (6 property tests: `truncate_output` bounds/prefix/next_offset invariants, `validate_path_safety` NUL-byte/whitespace rejection). Added 6 new unit tests for previously-untested error paths (`file_ops` nonexistent-target errors, `search_ops` invalid-regex/nonexistent-root errors, `text_ops` missing-column/missing-JSON-path/invalid-JSON errors). While touching every test module, also fixed a pre-existing `CODING.md` violation ("Strict Test Separation"): extracted all inline `#[cfg(test)] mod tests { ... }` blocks in `guardrails`, `file_ops`, `search_ops`, `text_ops`, `sys_ops` into companion `tests.rs` files (mechanical move, no logic changes), which also brought `file_ops/mod.rs` and `text_ops/mod.rs` back toward the 300-line production-file limit (still 374/270 respectively — `file_ops/mod.rs` remains over the limit due to AWU 4.3's doc comments; splitting production logic further was judged out of scope for this AWU). Total suite: 27 unit + 6 property + 7 integration + 17 doctests, all passing; `cargo fmt`/`clippy` clean. |

| **6.2** | **CI/CD Pipeline Setup** | `[✅] Completed` | Added `.github/workflows/ci.yml` (checkout → stable toolchain w/ rustfmt+clippy → cargo cache → `fmt --check` → `clippy --workspace --all-targets -- -D warnings` → `build` → `test`), triggered on push/PR to `main`. This exposed the one pre-existing clippy warning (`await_holding_lock` in a `sys_ops` test holding `ENV_MUTEX` across an `.await`) as a real CI blocker: fixed by switching `ENV_MUTEX` from `std::sync::Mutex` to `tokio::sync::Mutex` (sync tests now use `.blocking_lock()`, the async test uses `.lock().await`), which is the correct fix rather than suppressing the lint. `cargo clippy --workspace --all-targets -- -D warnings` now passes cleanly. README documents the CI pipeline. |
| **7.1** | **Optional Workspace-Root Confinement** | `[✅] Completed` | Design review raised that this MCP is meant to run scoped to `~/projects/rad`, and that a real process-isolation "sandbox" is disproportionate for single-user local use — the higher-value, cheaper investment is confining file operations to an intended directory. Added `AI_WORKSPACE_ROOT` (opt-in, off by default) to `guardrails::validate_path_safety`: when set, every path-validated call is lexically resolved (handles `..`/`.` and not-yet-existing targets without touching disk) and rejected if it falls outside the canonicalized root. Wired in centrally, so `file_ops`, `search_ops`, and `text_ops` all get it for free. Added 3 new unit tests, plus `ENV_MUTEX` locking to ~14 previously-unlocked tests across `guardrails`/`file_ops`/`search_ops`/`text_ops` to prevent the new global env var from racing unrelated concurrent tests (verified stable across 5 repeated `cargo test` runs). Explicitly does **not** cover `execute_command_in_sandbox`, which remains entirely unconfined — a conscious tradeoff, not an oversight (see 7.2). Manually verified end-to-end via the compiled binary. |
| **7.2** | **Correct "Sandbox" Terminology to Match Reality** | `[✅] Completed` | The design review found the "sandbox" framing overstated what `execute_command_in_sandbox` actually provides (timeout + stdout-size guard only — no process isolation), and that `extract_code_skeleton`'s README description falsely claimed tree-sitter parsing (it's regex-only). Reworded the MCP `tools/list` description and README entries for both to state plainly what they do and don't do, without renaming the tool (avoids breaking existing client configs). README's path-safety section now also documents `AI_WORKSPACE_ROOT` and states explicitly that guardrails are a mistake-prevention guard, not an adversarial security boundary, and that `execute_command_in_sandbox` bypasses them entirely. |
| **7.3** | **Remove `extract_code_skeleton`** | `[✅] Completed` | Discussing whether to invest in tree-sitter for this tool surfaced a deeper scope question: unlike every other tool (uniform, deterministic, language-agnostic — true to the "core utilities" identity), a correct code-skeleton extractor needs per-language grammar awareness, which is a fundamentally different (and open-ended) maintenance burden. The user decided it doesn't belong in this project and to drop it rather than invest in tree-sitter. Removed the function (`text_ops`), its test, its MCP `tools/list` entry and dispatch arm (`main.rs`), and its README entry; renumbered the remaining 14. Updated the integration test's tool-count assertion (15 → 14). Suite: 29 unit + 6 property + 7 integration + 17 doctests, all passing; `cargo fmt`/`clippy -D warnings` clean. Manually verified via the compiled binary that `tools/list` now returns exactly 14 tools and no longer includes `extract_code_skeleton`. |
| **7.4** | **`execute_command_in_sandbox` Timeout & Working-Directory Support** | `[✅] Completed` | The user confirmed this tool is modeled after a coding agent's bash tool and is load-bearing (removing/restricting it would cripple the agent), but flagged the fixed 5s timeout and lack of a working-directory option as real functional limits (most real dev commands — builds, installs, test suites — exceed 5s). Added `timeout_seconds: Option<u64>` (falls back to `AI_COMMAND_TIMEOUT_SECONDS`, default 30, clamped to 300) and `working_directory: Option<&str>` (defaults to `AI_WORKSPACE_ROOT` if set) to `execute_command_in_sandbox`; both new plumbing lives in two small helpers (`resolve_timeout`, `build_command`) to keep the function within `CODING.md`'s length guidance. Deliberately did **not** path-validate `working_directory`, for consistency with this tool's documented nature as a fully unconfined escape hatch (see AWU 7.1/7.2 and `ARCHITECTURE.md`). Updated `main.rs`'s `ExecCmdArgs`/schema/dispatch, README, and `ARCHITECTURE.md` (now describes these knobs in present tense). Added 4 new tests (explicit `working_directory`, `AI_WORKSPACE_ROOT` default, `resolve_timeout` clamping) and rewrote the timeout test to use a 1s override instead of waiting out a real 5s window — faster and no longer silently invalid now that the default is 30s. Suite: 32 unit + 6 property + 7 integration + 17 doctests, all passing (stable across 3 repeated runs); `cargo fmt`/`clippy -D warnings` clean. Manually verified both new parameters end-to-end via the compiled binary. |
| **7.5** | **Rename `execute_command_in_sandbox` → `execute_command`** | `[✅] Completed` | With every description now stating plainly that this tool isn't sandboxed (AWU 7.2/7.4), the name itself was the last place still claiming otherwise. The user agreed the name should match the honest description; renamed the Rust function, the MCP tool name, and all mentions in `README.md`/`ARCHITECTURE.md` (including the `sys_ops` module doc comment and the README's "System & Sandbox" section header, now "System & Shell Execution"). Left historical `PLANS.md` entries (this one included, above) under the old name, since they describe what was actually true when written. Suite: 32 unit + 6 property + 7 integration + 17 doctests, all passing; `cargo fmt`/`clippy -D warnings` clean. Manually verified via the compiled binary that `tools/list` reports `execute_command` and a `tools/call` against it succeeds. |
| **7.6** | **Fix `validate_path_safety` rejecting `.` for read-only operations, close a `./`-spelling bypass, and add remediation hints to guardrail errors** | `[✅] Completed` | User-reported bug: a `rad` agent session calling `list_directory_contents(".")` was rejected by `validate_path_safety`'s hard-coded `trimmed == "."` check, even though `.` is `list_directory_contents`'s own documented default — the LLM had no way to satisfy the tool at all, retried the identical call, then burned context on an `execute_command("pwd")` detour before the session stalled. Root-caused to `validate_path_safety` being applied uniformly to both mutating operations (delete/move/copy-dest/mkdir/edit, where `.` is genuinely dangerous) and read-only ones (list/stat/read/search, where `.` is the normal, safe default). Split the function: `validate_path_safety` (mutation guard, still rejects `.`) and new `validate_read_path_safety` (permits `.`); refactored shared checks into a private `common_path_checks` helper to avoid duplication. Repointed the read-only call sites — `file_ops::list_directory_contents`/`get_file_metadata`, both `search_ops` functions, all three `text_ops` functions — to the new function. Also fixed a related gap found during design review: the old literal `trimmed == "."` comparison ran before any lexical normalization, so equivalent spellings (`./`, `././`, `a/..`) silently bypassed the check entirely; replaced it with `is_current_dir_equivalent`, which lexically collapses `.`/`..` components (reusing the existing `lexically_normalize` helper) before comparing. Finally, since tracing the original incident showed the LLM received only `"Error: Guardrail violation: dangerous path sequence '.'"` with no reason or remediation, every guardrail rejection message (current-dir, root/wildcard/tilde, glob pattern, critical system dir, workspace-root confinement) now states what to pass instead. Added 3 new unit tests (`test_path_safety_rejects_current_dir_spellings`, `test_read_path_safety_permits_current_dir`, plus a NUL-byte case on the read path) and 2 new doctests. Suite: 34 unit + 6 property + 7 integration + 18 doctests, all passing; `cargo fmt`/`clippy --all-targets` clean. |
| **7.7** | **Add remediation hints to the highest-value non-guardrail errors** | `[✅] Completed` | Follow-on from AWU 7.6: reviewed every `CoreError` call site in the crate to decide where else an LLM caller lacks the information to self-correct. Most are already fine as-is — either self-explanatory (`"Path does not exist: {path}"`) or already pass through an already-detailed underlying error (OS/regex/serde_json/csv messages) where a canned hint would be redundant or risk being wrong; `edit_file_content`'s content-mismatch error already echoes both expected and actual content, which is itself the best possible hint. Found and fixed exactly 3 genuine gaps: (1) `sys_ops::execute_command`'s timeout error stated only `"Command execution timed out"` with no indication of the timeout used or that a larger `timeout_seconds` argument is available (now: `"...timed out after {n}s — pass a larger timeout_seconds argument (up to {MAX_TIMEOUT_SECS}) ..."`); (2) `text_ops::filter_and_sort_matrix_columns` reported `"None of the specified columns were found"` without saying which headers *do* exist, so a typo'd column name gave the LLM nothing to retry with (now lists the requested columns and the file's actual headers); (3) `text_ops::query_json_by_path`'s `"Path '{}' not found in JSON object"` gave no indication of where in a nested structure resolution broke (now walks the JSON pointer segment-by-segment and reports the deepest resolved location plus what's available there — an object's keys, an array's length, or a scalar's value). Tightened the 3 corresponding tests to assert on the new message content rather than just the error variant. Suite: 34 unit + 6 property + 7 integration + 18 doctests, all passing; `cargo fmt`/`clippy --all-targets` clean. |
| **7.8** | **Sync `README.md`/`ARCHITECTURE.md` with the `validate_read_path_safety` split** | `[✅] Completed` | User asked for a documentation-consistency check after AWU 7.6/7.7. Found both docs still described path safety as a single, uniform check that always rejects `.` — exactly the misconception AWU 7.6 fixed in the code, now stale in the docs themselves. Updated README's "Path Safety Validation" section to describe the two validators separately (destructive commands still reject `.` in any lexically-equivalent spelling; read-only commands permit it) and to name which tools use which. Updated ARCHITECTURE.md's "Water-Edge Path Defense" section the same way, corrected its claim that path checks are done "string-by-string" (the `.`-equivalence check is now lexical-normalization-based, not a literal comparison), and fixed the Mermaid dependency diagram, which listed only `validate_path_safety` and had never shown `search_ops` depending on `guardrails` at all despite always using `truncate_output` (and, as of 7.6, `validate_read_path_safety`) — a pre-existing inaccuracy predating this AWU, caught in the same pass. No code changes. |
| **7.9** | **Full codebase audit against `CODING.md`/`AUDITING.md`; fix everything found** | `[✅] Completed` | User asked for a full-codebase compliance sweep, then to fix everything found. Findings and fixes: **(1) Unused dependencies** — `anyhow`/`sha2`/`hex` in `core-utilities-mcp-lib` and `thiserror` in `core-utilities-mcp` were declared but never referenced (likely leftover from the removed checksum-verification idea and from `CoreError` re-use); removed all 4. **(2) Unjustified `#[allow(dead_code)]`** on `JsonRpcRequest::jsonrpc` — added a comment explaining the field is kept only so requests missing the mandatory JSON-RPC "jsonrpc" key fail to deserialize, not because the value is read. **(3) 40-line function-length violations** — refactored `query_json_by_path` (76→21, extracted `dot_path_to_json_pointer`/`describe_pointer_resolution_failure`), `filter_and_sort_matrix_columns` (68→32, extracted `resolve_column_indices`/`read_filtered_rows`/`format_matrix_output`), `edit_file_content` (61→35, extracted `verify_line_range`/`atomic_write`), `search_text_with_limit` (55→20) and `search_file_by_name_or_type` (55→35, extracted `build_matcher`/`collect_files_to_scan`/`find_matches`/`entry_matches` in `search_ops`), `get_file_metadata` (45→31, extracted `epoch_secs` to dedupe the modified/accessed timestamp handling), and `execute_command` (90→30, extracted `spawn_with_pipes`/`read_output_streams`/`build_command_result`). Left `list_directory_contents` (41, one line over a single cohesive loop) as-is per `CODING.md`'s own anti-fragmentation clause. **(4) File-length violations** — `file_ops/mod.rs` (374, already known debt) split into `file_ops/mod.rs` (delete/list/stat, 169 lines) and new `file_ops/mutate.rs` (copy/move/create/edit, 241 lines; re-exported so `file_ops::copy_file_or_directory` etc. keep working), each with its own companion `tests.rs`. `core-utilities-mcp/src/main.rs` (709 lines, not previously logged) split into `main.rs` (222, entry point + JSON-RPC loop + per-method routing), `rpc_types.rs` (109, JSON-RPC/argument structs), `tools.rs` (178, the `tools/list` schema table), and `dispatch.rs` (218, `tools/call` resolution). This also shrank `handle_request` from 451 to 18 lines by splitting it into `handle_initialize`/`handle_tools_list`/`handle_tools_call`. Left `tool_definitions` (170 lines) and `dispatch_tool_call` (122 lines) over the function-length guidance, each with an in-code comment explaining why: both are single data/dispatch tables (one JSON schema literal, one "deserialize→call→stringify" match repeated per tool) where splitting would scatter one coherent table across many artificial one-arm functions — exactly what `CODING.md`'s anti-fragmentation clause warns against. **(5) README sync** — updated the repository-structure tree for both splits and to add the previously-missing `sys_ops/` entry. Also confirmed and left as-is: `clippy::pedantic` is not actually enforced anywhere (CI runs default `-D warnings` only) despite `AUDITING.md` describing it as a requirement — flagged to the user as a documentation/reality gap, not fixed (enabling it would be a much larger, separate change: a pedantic-clean sweep found ~81 warnings). Suite: 35 unit + 6 property + 7 integration + 18 doctests, all passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings` clean throughout. Manually verified via the compiled binary that behavior is unchanged (re-ran the integration suite, which drives the real binary over stdin/stdout). |

---

| **7.10** | **Wire `check_secrets.sh`/`check_licenses.py` into a pre-commit hook and CI** | `[✅] Completed` | Both scripts existed in `scripts/` but were never actually invoked anywhere — not in CI, not as a git hook. Added both to `.github/workflows/ci.yml` (`check_secrets.sh --all` right after checkout, `check_licenses.py` after the Rust toolchain step since it needs `cargo metadata`) as a backstop that runs regardless of local setup. Also added `.githooks/pre-commit` (tracked in git, since `.git/hooks/` isn't) running `check_secrets.sh` in its default staged-only mode, since catching a secret before it's committed is far more valuable than catching it in CI after it's already in history — this exact gap is what forced the git-history rewrite the user just went through for AWU-adjacent personal-info exposure. Documented the one-time `git config core.hooksPath .githooks` opt-in in README's new "Git Hooks" subsection, since git doesn't apply hooks from a repo-relative path automatically. Running `check_secrets.sh --all` for the first time surfaced 3 pre-existing false-triggers: illustrative `/Users/me/...` paths in `guardrails::validate_path_safety`'s doctest examples (the scanner's `.md`/`.agents/`/`test*.rs` exclusions don't cover doc-comment examples in production files) — changed to `/srv/rad`-style paths, which don't match the `/Users/`/`/home/` patterns and don't change the doctest's meaning (`no_run`, so the paths don't need to exist). Verified end-to-end: staged a fake `sk-ant-...` string and confirmed the hook actually blocks the commit before reverting it. Suite: 35 unit + 6 property + 7 integration + 18 doctests, all passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings` clean; both scripts pass on the current tree. |
| **7.11** | **Add `write_file` tool** | `[✅] Completed` | Reviewing the project against its own "core utilities for an agent" mission surfaced a real functional gap: `edit_file_content` only operates on existing files, `create_directory` only makes directories, so there was no structured way to create a new file's content at all — the only path was falling back to `execute_command` (`echo ... > file`), defeating the point of a deterministic, non-shell tool surface for one of the most basic agent actions. Added `write_file(path, content, overwrite)` to `file_ops/mutate.rs`, reusing the existing private `atomic_write` helper for the actual write. Refuses to overwrite an existing file unless `overwrite: true` is passed (mirrors `edit_file_content`'s verify-then-write safety philosophy, since a plain unconditional write could silently clobber content the caller didn't mean to touch), and auto-creates missing parent directories (matching `copy_file_or_directory`/`move_file_or_directory`'s existing behavior for destinations). Wired through `file_ops/mod.rs`'s re-export, `rpc_types::WriteFileArgs`, `tools.rs`'s schema, and a new `dispatch.rs` arm (using strict argument parsing like `edit_file_content`, since a default empty `content` would silently write an empty file rather than surface a malformed request). Added 2 unit tests (new-file-with-parent-creation, overwrite-refused-then-allowed) and an end-to-end integration test driving the real binary over stdin/stdout. Updated README's tool table (14 → 15) and the integration test's tool-count assertion. Suite: 37 unit + 6 property + 8 integration + 19 doctests, all passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings` clean. |
| **7.12** | **Enforce `clippy::pedantic`, closing the `AUDITING.md` gap from AWU 7.9** | `[✅] Completed` | AWU 7.9 found `AUDITING.md` describes `clippy::pedantic` as a requirement that nothing actually enforces (~81 warnings existed). `cargo clippy --fix -- -W clippy::pedantic` mechanically resolved the bulk of them (54 were `uninlined_format_args` alone). The remaining 9 needed manual fixes: renamed two confusable `matches`/`matcher` bindings in `search_ops` to `results`; added `# Errors` sections to `validate_path_safety`/`validate_read_path_safety` (the prose already documented the failure conditions, just not under the heading pedantic's `missing_errors_doc` looks for); changed `filter_and_sort_matrix_columns`'s `columns: Vec<String>` parameter to `&[String]` (never consumed, only ever borrowed) and its call site in `dispatch.rs`; made the `.tsv` extension check case-insensitive; changed `invalid_params_response`'s `error` parameter to `&serde_json::Error` (only formatted, never moved); added `#[allow(clippy::too_many_lines)]` with a one-line pointer to each function's existing "single data/dispatch table" justification comment on `tool_definitions` and `dispatch_tool_call` (same functions already exempted from `CODING.md`'s 40-line guidance in AWU 7.9, so the pedantic-level 100-line threshold gets the same rationale rather than a new one). Fixing `guardrails/mod.rs`'s two new `# Errors` sections pushed it to 309 lines, just over the 300-line limit; trimmed several doc comments' wording (no meaning lost) back down to exactly 300 rather than splitting the module further. Wired `-D clippy::pedantic` into CI's clippy step, alongside the existing `-D warnings`, so this is now actually enforced rather than aspirational. Suite: 37 unit + 6 property + 8 integration + 19 doctests, all passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean; all files ≤300 lines. |
| **7.13** | **Fresh full-codebase/docs audit: remove dead `smart_boundary` param, fix 4 stale docs** | `[✅] Completed` | User asked for another full pass over the codebase and docs post-7.9–7.12. Code itself was clean (fmt/pedantic clippy/tests/secrets/license scan all green — the earlier AWUs held). Found 5 issues: **(1)** `read_file_with_limit`'s `smart_boundary` parameter was threaded through the entire call chain (`tools.rs` schema → `rpc_types::ReadArgs` → `dispatch.rs` → the function itself) but the function parameter was `_smart_boundary`-prefixed and never read — `truncate_output` always does smart-boundary truncation regardless, so the API advertised a control that didn't exist. Discussed with the user whether to implement or remove it; implementing would mean threading a mode flag through the shared `truncate_output` helper (used by nearly every tool) for a use case nobody had, so removed the parameter entirely instead — from the schema, the args struct, the dispatch call, and the function signature/doctest/tests. **(2)** README's CI description hadn't been updated when `-D clippy::pedantic` was added to CI in AWU 7.12. **(3)** `ARCHITECTURE.md` never mentioned `sys_ops` at all — missing from both the "Organized into:" module list and the Mermaid diagram (including its `truncate_output` dependency) — a gap that had survived two earlier doc-sync passes (AWU 7.8, 7.9) without being caught. **(4)** `lib.rs`'s crate-level doc comment listing mutating operations ("delete, move, copy destination, mkdir, edit") hadn't been updated for `write_file` (AWU 7.11). **(5)** Three stray blank lines in README before the final `---` of the tool list section — cosmetic, fixed while already in the file. Suite: 37 unit + 6 property + 8 integration + 19 doctests, all passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean; secrets/license scans clean; manually verified `read_file_with_limit` still works via the compiled binary post-removal. |
| **7.14** | **Close the tool-scope review; declare v1.0.0** | `[✅] Completed` | Re-examined all 15 tools against `ARCHITECTURE.md`'s "Uniformity Litmus Test" (deterministic and uniform regardless of content; no open-ended format/language intelligence required — the standard `extract_code_skeleton` was cut against, AWU 7.3). The two tools most analogous to that precedent, `filter_and_sort_matrix_columns` (CSV/TSV) and `query_json_by_path` (JSON), pass: both are single, unambiguous, mechanically-parseable grammars (via `csv`/`serde_json`), unlike per-language code structure, which has no shared canonical grammar. `execute_command` was already explicitly re-affirmed as an intentional exception in AWU 7.4/7.5. No tool warranted removal. User agreed and declared the project version 1.0.0: bumped both crates' `Cargo.toml` versions, `main.rs`'s `--version` output, and the MCP `initialize` response's `serverInfo.version`, all `0.1.0` → `1.0.0`. Verified via the compiled binary that both now report `1.0.0`. Suite: 37 unit + 6 property + 8 integration + 19 doctests, all passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean. |
| **7.15** | **Migrate MCP result construction to typed `rust-mcp-schema` (0.10.3)** | `[✅] Completed` | This is the first AWU driven by real usage rather than static review (see 7.14's note): `rad` (the client project spawning this server) had migrated its own JSON-RPC/MCP handling to `rust-mcp-schema` typed structs and bumped `protocolVersion` from the 2-generations-stale hardcoded `"2024-11-05"` to `ProtocolVersion::latest()` (`"2025-11-25"`); this server needed to follow suit. Studied `rad`'s usage pattern first (`~/projects/rad/ext/mcp-tool-provider/src/{client.rs,lib.rs}`), then verified every type against the actual vendored source (`~/.cargo/registry/.../rust-mcp-schema-0.10.3/src/generated_schema/2025_11_25/`) rather than trusting doc summaries. Added `rust-mcp-schema = "0.10"` to the bin crate only (pulls in no async-runtime dependency). `handle_initialize` now builds `InitializeResult`/`ServerCapabilities`/`Implementation` typed and reads `protocolVersion` from `ProtocolVersion::latest().to_string()` instead of a hardcoded string. `handle_tools_list` now deserializes `tools::tool_definitions()`'s JSON into `Vec<Tool>` before wrapping it in `ListToolsResult` — this makes the static schema table's shape actually validated against the real MCP `Tool`/`ToolInputSchema` types at runtime, where before a typo would have just produced a malformed-but-unnoticed response; a deserialize failure now surfaces as a `-32603` JSON-RPC error rather than panicking. `dispatch.rs`'s `build_tool_call_response` now builds `CallToolResult`/`ContentBlock::TextContent` via `TextContent::new(text, annotations, meta)`; this moved the error-category field from a bespoke top-level `error_type` key on the content block into `_meta.error_type`, the spec's actual extension-field location (verified `rad`'s client code only reads `.text` off content blocks and never consumed the old field, so this is a safe wire-format correction, not a breaking change in practice) — updated the one integration test asserting on it and README/ARCHITECTURE.md's docs of the new field location. Deliberately did **not** adopt `rust-mcp-schema`'s `ServerJsonrpcResponse`/`JsonrpcErrorResponse` for the outer envelope: unlike our single `JsonRpcResponse` (which unifies success/error via `Option` fields), those are two separate types with no shared success/error type, and switching would have meant restructuring `handle_request`'s return type across every call path for no behavioral gain — kept `rpc_types.rs`'s hand-rolled JSON-RPC 2.0 envelope exactly as before, per explicit instruction that the transport layer didn't need to change. Added a small `result_response()` helper (wraps any typed result in `ResultFromServer`, an untagged enum, before serializing into the envelope — costs nothing on the wire, keeps every success path going through a real MCP-result type). Verified: `cargo build`/`--release`, `clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean, full suite (37 unit + 6 property + 8 integration + 19 doctests) passing, secrets/license scans clean, manual JSON-RPC smoke tests of `initialize`/`tools/list`/successful and failing `tools/call`, and a real end-to-end run through the compiled `rad` binary (`[OK] Verified 15 tools from extension 'mcp-tool-provider']`, a `list_directory_contents` call round-tripped correctly). |
| **7.16** | **Global MCP Configuration Clean-up to `core-utilities-mcp`** | `[✅] Completed` | Updated global config files (`~/.pi/agent/mcp.json`, `~/.rad/config.json`, and `~/.gemini/config/mcp_config.json`) to map solely to `/Users/akahmys/.cargo/bin/core-utilities-mcp`. Removed all legacy references (`mcp-uutils` and `mcp-uutils-mcp`), deleted `~/.cargo/bin/mcp-uutils-mcp` symlink, and cleaned up stale build artifacts in `target/`. Verified via stdio JSON-RPC initialization. |
| **8.1** | **Fix Potential Stream Truncation Bug in `sys_ops::read_output_streams`** | `[✅] Completed` | Refactored `read_output_streams` to track `stdout` and `stderr` EOF flags independently via `tokio::select!`. Added unit test `test_execute_command_captures_stderr_after_stdout_close` verifying `stderr` output capture. |
| **8.2** | **Unique Temporary File Names in `file_ops::mutate::atomic_write`** | `[✅] Completed` | Updated `atomic_write` to use process ID (`std::process::id()`) and an atomic counter (`TEMP_COUNTER`) for temporary file paths (`.{file}.tmp.{pid}_{seq}`), resolving collision and overwrite risks. |
| **8.3** | **Memory & Stream Optimization for `search_ops` & `text_ops`** | `[✅] Completed` | Optimized `read_file_with_limit` to bound character collection via `take(limit + 1024)` on `BufReader`. Capped `search_ops::find_matches` at 1000 items to prevent unbounded memory allocation. |
| **8.4** | **Path Verification & Existence Helper** | `[✅] Completed` | Added `ensure_existing_read_path` to `guardrails`. Refactored `get_file_metadata`, `search_text_with_limit`, `search_file_by_name_or_type`, `read_file_with_limit`, `filter_and_sort_matrix_columns`, and `query_json_by_path` to eliminate duplicated validation and existence-check boilerplate. |
| **8.5** | **Streamlined Deserialization in `dispatch.rs`** | `[✅] Completed` | Added `#[derive(Default)]` to all argument structs in `rpc_types.rs`. Replaced verbose `unwrap_or(...)` instantiations in `dispatch.rs` with `unwrap_or_default()`. |
| **8.6** | **Test Suite Hardening** | `[✅] Completed` | Protected `property_tests.rs` environment variable removal with `ENV_MUTEX.blocking_lock()`. Added `tools_call_write_file_refuses_overwrite_without_flag` integration test. All 72 tests pass. |
| **8.7** | **Reorder README sections** | `[✅] Completed` | Reordered `README.md` to place 15 Core Command Specification immediately after introduction. |
| **8.8** | **Reorder README for user-centric flow** | `[✅] Completed` | Moved `Common Guardrails` above `Repository Structure` to unify user-facing documentation before developer-facing documentation. |
| **8.9** | **Fix CI clippy failure caused by upstream stable-toolchain drift** | `[✅] Completed` | User reported the GitHub Actions workflow failing. CI's clippy step (`-D warnings -D clippy::pedantic`) uses `dtolnay/rust-toolchain@stable`, which always installs whatever "stable" currently is — it had moved from 1.96.0 to 1.97.1 since this passed locally, and 1.97 added a new pedantic lint (`clippy::manual_assert_eq`) that our stale local toolchain couldn't see. Confirmed via `rustup check` (update available) rather than guessing from unreadable CI logs (the Actions API needs admin/token auth to fetch job logs, which wasn't available here — `rustup update stable` + reproducing locally was the reliable path). Fixed the one flagged line: `sys_ops::tests::test_get_system_context`'s `assert!(ctx["os"] != serde_json::Value::Null)` → `assert_ne!(...)`. No other pedantic violations surfaced. This is a real recurring-risk class, not a one-off: CI tracking a moving "stable" channel means it can start failing on an untouched commit purely from upstream clippy additions, independent of any code change here — worth keeping in mind if it happens again. Suite: 38 unit + 6 property + 9 integration + 19 doctests, all passing under the updated 1.97.1 toolchain; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean; secrets/license scans clean. |
| **8.10** | **Absorb `file-edit-mcp`'s Edit/Read Improvements** | `[✅] Completed` | `file-edit-mcp` (a same-day fork of this project, scoped down to 4 then 3 tools purely for safe file editing) had independently improved its `edit`/`read` logic well past what this project still had. Rather than maintain two diverging copies, decided to fold those improvements back in here and retire `file-edit-mcp` as a separate project — this repo becomes the single comprehensive MCP (all 15 tools) for lighter-weight agent consumers (`rad`, `pi-coding-agent`) that lack native search/shell/file tools, unlike e.g. Claude Code. Compared both implementations function-by-function first: `file-edit-mcp` had multi-chunk atomic edits (`edits` array, overlap checking), a `line_delta` response field, CRLF normalization before line-splitting (this project's old `edit_file_content` split on bare `\n`, risking mixed line endings after editing a CRLF file), and per-line trailing-whitespace-tolerant content verification (this project's old comparison only did one `.trim()` over the whole joined block). This project's own `edit_file_content`/`read_file_with_limit` had things `file-edit-mcp` had *lost*: unique per-call temp filenames in `atomic_write` (PID + atomic counter, from AWU 8.2 — `file-edit-mcp`'s version regressed to a fixed `.tmp` name) and a bounded read window (`limit + 1024` chars, from AWU 8.3 — `file-edit-mcp`'s new line-annotated `read_file` had regressed this too, annotating every remaining line up to EOF before truncating). Also considered and rejected porting over a `lines_modified` response field: it's fully derivable by the caller from the `start_line`/`end_line` it already submitted, so it adds token cost for zero new information — unlike `line_delta`, which the caller genuinely cannot compute itself. Net result: renamed `edit_file_content` -> `edit_file` (now multi-chunk, with `line_delta`, keeping this project's unique-temp-filename `atomic_write` and CRLF/whitespace fixes ported in from `file-edit-mcp`) and `read_file_with_limit` -> `read_file` (now 1-indexed `start_line` instead of a character `start_offset`, each line annotated `"{n}\t{content}"`, re-added the bounded-accumulation window as `READ_WINDOW_MARGIN` so a huge file's remainder still isn't annotated in full before truncating — added `test_read_file_bounds_accumulation_for_huge_files`, a 200k-line regression test, to lock that back in). `copy_file_or_directory`/`move_file_or_directory`/`create_directory`/`delete_file_or_directory`/`list_directory_contents`/`get_file_metadata`/`search_ops`/`sys_ops`/`filter_and_sort_matrix_columns`/`query_json_by_path` were untouched — this project never dropped them, unlike `file-edit-mcp`. Added end-to-end integration coverage for `edit_file`/`read_file` (neither tool pair ever had any, in either project — only unit tests). Suite: 11 integration + 43 unit + 6 property + 19 doctests = 79 tests, all passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean; secrets/license scans clean. Manually smoke-tested `read_file`/`edit_file` against the compiled binary over real stdin/stdout, and confirmed `tools/list` still reports all 15 tools by name. |
| **8.11** | **Command Composition Review** | `[✅] Completed` | User asked for a full reconsideration of the 15-tool composition — names, per-tool design, and whether the tool *set* itself was right. Findings and fixes: **(1) Real bug** — `search_text_with_limit`'s match results ran `line.trim()` (both ends), but `edit_file`'s verification only trims trailing whitespace per line, preserving leading indentation. Pasting a search match's `content` straight into `edit_file`'s `target_content` for indented code would fail verification. Fixed to `line.trim_end()`; added `test_search_text_preserves_leading_whitespace` regression test. **(2) Schema quality** — the 12 pre-existing tools mostly had bare `{"type": "string"}` params with no `description`, unlike `edit_file`/`read_file`/`write_file`; added per-parameter descriptions throughout `tools.rs` for consistency, since the target audience (lightweight/local LLMs) benefits more from explicit schema documentation than a model that reasons well from a short tool-level blurb alone. **(3) Naming** — considered shortening all 12 verbose names (e.g. `copy_file_or_directory` -> `copy_file`, `query_json_by_path` -> `query_json`), but caught that several of these drop real capability signal rather than just verbosity: `_or_directory` tells the caller the tool handles directories too (dropping it risks the caller thinking it's file-only and avoiding it for directory operations), and `_by_path` on the JSON query tool signals its specific dot-path querying mechanism (dropping it, paired with a hypothetical `query_csv`, would make two differently-powered tools look like symmetric siblings). Also rejected `get_file_metadata` -> `stat_file` (assumes Unix `stat` familiarity a lightweight/non-Unix-trained model may lack). Renamed only the 5 where shortening loses no decision-relevant information: `list_directory_contents` -> `list_dir`, `create_directory` -> `create_dir`, `search_text_with_limit` -> `search_text`, `search_file_by_name_or_type` -> `find_files`, `get_system_context` -> `get_system_info`. Left `copy_file_or_directory`/`move_file_or_directory`/`delete_file_or_directory`/`get_file_metadata`/`filter_and_sort_matrix_columns`/`execute_command` as-is. **(4) Tool-set composition** — reviewed whether any category was redundant or missing; concluded the 15-tool set (file mutation, file read/inspect, search, structured-data query, system, shell) is appropriate for the confirmed lightweight-agent audience, no additions/removals needed, except: **(5) New capability** — `query_json_by_path` only handled JSON, but config files a coding agent actually encounters (`Cargo.toml`, `docker-compose.yml`, GitHub Actions YAML) are more often TOML/YAML. Renamed to `query_data_by_path`; added `toml`/`serde_yaml` dependencies and a `parse_structured_data` helper that detects format from the file extension (`.toml`, `.yml`/`.yaml`, else JSON) and normalizes all three into `serde_json::Value` so the existing dot-path resolution logic (`dot_path_to_json_pointer`, `describe_pointer_resolution_failure`) is reused unchanged. `QueryJsonArgs`/`json_path` renamed to `QueryDataArgs`/`data_path` throughout `rpc_types.rs`/`dispatch.rs`. Added 3 new unit tests (TOML nested-array access, YAML including a list index, `.yaml` extension variant) and an end-to-end integration test covering both formats (this tool never had integration coverage before, in either format). **Note**: `serde_yaml` is an archived/deprecated crate (upstream stopped maintaining it in 2024, though it still functions and passed the license scan) — flagged to the user as a maintenance risk worth revisiting, not silently accepted. Suite: 12 integration + 47 unit + 6 property + 19 doctests = 84 tests, all passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean; secrets/license scans clean. Manually smoke-tested `query_data_by_path` against a real `.toml` file via the compiled binary, and confirmed `tools/list` reports the renamed tools. |
| **8.12** | **Replace `serde_yaml` with `yaml-rust2`** | `[✅] Completed` | Follow-on from AWU 8.11's note flagging `serde_yaml` as archived/deprecated. Researched alternatives via web search rather than guessing: `serde_yml` (an initially-plausible-looking successor) turned out to have an actual RustSec advisory (RUSTSEC-2025-0068, unsound and unmaintained) — avoided. Of the two established maintained forks, chose neither: `serde_yaml_ng` still sits on the same unmaintained `unsafe-libyaml` C bindings the original depended on (same failure mode deferred, not fixed), and `serde_norway`, while maintaining its own fork of those C bindings, isn't pure Rust either. User asked for a pure-Rust option specifically. Considered and rejected `noyalib` (pure Rust, zero `unsafe`, technically appealing) because its author is the same person behind the unsound `serde_yml` — one prior unsoundness incident from that maintainer was reason enough to prefer a more established alternative for a project whose whole premise is safety guardrails, despite noyalib's much lower download/track record being the more immediate tell. Landed on `yaml-rust2`: pure Rust, no `unsafe`, a long-established lineage (forked from the original `yaml-rust` after RUSTSEC-2024-0320 flagged *that* as unmaintained, under a different, unrelated maintainer), and no serde `Value` integration of its own. Also considered and rejected forking `yaml-rust2` or writing a from-scratch YAML parser: full YAML 1.2 compliance (quoting rules, block/flow styles, anchors/aliases, multi-line string folding, implicit typing, multi-document separators) is a large, ongoing maintenance burden wildly disproportionate to this being a small config-file-query convenience feature — exactly the kind of speculative over-engineering `CODING.md` warns against. Used `yaml-rust2` as a normal dependency instead and wrote the missing glue myself: `yaml_to_json_value` (recursively converts `yaml_rust2::Yaml` into `serde_json::Value` — `Real`/`Integer`/`String`/`Boolean`/`Array`/`Hash` map directly, `Alias`/`Null`/`BadValue` all collapse to `Value::Null` since a freshly-parsed document never legitimately produces `BadValue`, that variant only appears from invalid indexing) and `yaml_key_to_string` (stringifies a YAML mapping key for use as a JSON object key, since JSON keys must be strings; non-scalar keys are vanishingly rare in real config files and collapse to an empty string rather than panicking or being silently dropped). All existing TOML/YAML tests (unit + integration) needed no changes — same `query_data_by_path` public contract, only the internal parser swapped. Suite: 12 integration + 47 unit + 6 property + 19 doctests = 84 tests, all still passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean; secrets/license scans clean (confirmed `yaml-rust2` is dual MIT/Apache-2.0). Manually smoke-tested `query_data_by_path` against a real `.yml` file (nested map + array index) via the compiled binary. |
| **8.13** | **Full Codebase Audit (Incl. Tests)** | `[✅] Completed` | User asked for a full re-audit, explicitly including test code. Findings and fixes, all against `CODING.md` invariants clippy pedantic doesn't check: **(1)** 3 files were over the 300-line limit — `guardrails/mod.rs` (313), `file_ops/mutate.rs` (367), `text_ops/mod.rs` (382) — grown from the last several AWUs' additions. Split each along existing conceptual seams rather than arbitrarily: `guardrails` -> `path_safety.rs` + `truncation.rs` (path validation vs. output truncation were always two unrelated concerns sharing one file); `file_ops::mutate` -> `mutate/copy_move.rs` + `mutate/edit.rs` (copy/move/create vs. the line-range editor); `text_ops` -> `read.rs` + `matrix.rs` + `query.rs` (three genuinely different jobs: line-windowed reading, CSV filtering, structured-data querying). Each split file's companion `tests.rs` was split the same way (mechanical move, zero logic changes) rather than left as one shared test file, so test-to-production mapping stays 1:1. All `pub use` re-exports at each new `mod.rs` kept the same public names, so zero call sites elsewhere in the crate needed to change. **(2)** `edit_file` was 47 lines (limit 40) after AWU 8.10's merge; extracted the line-count/`line_delta` computation into `compute_line_stats` (8 lines), bringing it to exactly 40. `dispatch_tool_call` (86) and `tool_definitions` (192) remain over with their existing justification comments (single dispatch/data tables) — confirmed still accurate, left as-is. **(3)** `CoreError::Io` and `CoreError::Unexpected` had zero constructors anywhere (same dead-variant pattern found in `file-edit-mcp` during AWU 1.10, before that project was retired) — confirmed via a clean rebuild that removing `Io`'s `#[from] std::io::Error` broke nothing, since every actual `std::io::Error` in the crate is already explicitly `.map_err`'d into `CoreError::File`. Removed both variants and their `category()` arms. **(4)** `AGENTS.md`/`AUDITING.md` had the *exact* fictional-tool-name problem found and fixed in `file-edit-mcp` (AWU 1.9) — `peek_file`, `inspect_dir`, `structured_grep`, `spawn_bash_process`, `verify_file`, `file_edit_patch` — never fixed here because that fix happened on the fork, not the original. Reworded both to the real tool names (`read_file`, `list_dir`, `search_text`/`find_files`, `execute_command`, `get_file_metadata` for size/mtime + `execute_command` for line counts/hashes it doesn't report). **(5)** README's repository-structure tree and ARCHITECTURE.md's `text_ops` description hadn't been updated for the AWU 8.11 TOML/YAML addition or this AWU's submodule splits. Verified all `Cargo.toml` dependencies are still referenced (no orphans from the `edit_file`/`read_file`/`query_data_by_path` rewrites) and `ENV_MUTEX` usage is consistent (`.blocking_lock()` in sync tests, `.lock().await` only in `sys_ops`'s async tests — no `std::sync::Mutex`-style calls leaked in from the file-edit-mcp side of this session). Confirmed no duplicate test function names and every `pub fn` in the lib has at least one test referencing it. Suite: 12 integration + 47 unit + 6 property + 19 doctests = 84 tests, all passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean; secrets/license scans clean; clean rebuild (`touch`ed every `.rs` file) produces zero warnings. |
| **8.14** | **Documentation & Policy-File Review** | `[✅] Completed` | User asked for a dedicated pass over every doc/policy file (`README.md`, `ARCHITECTURE.md`, `AGENTS.md`, `AUDITING.md`, `CODING.md`, `PLANNING.md`, `PLANS.md`). Findings and fixes: **(1)** Both `README.md`'s opening description and `main.rs --help`'s output claimed the server is "powered by uutils/coreutils" — the exact same false claim found and fixed in `file-edit-mcp` back in AWU 1.9, except that fix landed on the fork and was never ported back to this, the original project (`grep`/`Cargo.lock` confirm zero actual `uutils` usage anywhere). Reworded both. **(2)** `--version` had a hand-typed `"1.0.0"` instead of `env!("CARGO_PKG_VERSION")` — same class of drift risk as the `uutils` claim, also already fixed once in `file-edit-mcp` and not ported back; fixed. **(3)** `--help` was missing `AI_COMMAND_TIMEOUT_SECONDS` and `AI_WORKSPACE_ROOT`, both real and documented elsewhere (README, ARCHITECTURE.md) but absent from the one place a caller running the binary directly would look first; added. **(4)** README's `Setup & Test` used `cargo test --all` while CI and every other doc-referenced command use `--workspace` (behaviorally identical, but inconsistent); aligned. **(5)** `PLANS.md`'s own "Next Steps" section was stale — still reporting AWU 8.9's 72-test count and pre-AWU-8.10 status, unread since. Updated to reflect AWU 8.10–8.13's actual outcome (84 tests, `file-edit-mcp` retirement, the reinstalled binary). Reviewed `ARCHITECTURE.md`, `CODING.md`, and `PLANNING.md` in full against the current codebase and found no other inaccuracies — `ARCHITECTURE.md`'s Mermaid diagram and safety-system descriptions reference only the stable public function names (`validate_path_safety`, `truncate_output`, etc.), which the AWU 8.13 submodule split didn't change, so no update was needed there. Suite: 84 tests, all still passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean; secrets/license scans clean. Manually verified the corrected `--help`/`--version` output against the compiled binary. |

---

## 🚀 Next Steps
All planned roadmap phases (Phases 1–8) are complete, including AWU 8.10–8.13's absorption of `file-edit-mcp`'s edit/read improvements, the command composition review (5 tool renames, TOML/YAML support via `query_data_by_path`), the `yaml-rust2` dependency swap, and the full codebase audit that followed (file/function-length splits, dead `CoreError` variants, stale docs). This repo is now the single comprehensive MCP (all 15 tools) intended for lightweight agent consumers (`rad`, `pi-coding-agent`); `file-edit-mcp` has been retired. Suite: 47 unit + 6 property + 12 integration + 19 doctests (84 total) passing; `cargo fmt`/`clippy --workspace --all-targets -- -D warnings -D clippy::pedantic` clean; secrets and license scans clean; `~/.cargo/bin/core-utilities-mcp` rebuilt and reinstalled with all AWU 8.10–8.13 changes.
